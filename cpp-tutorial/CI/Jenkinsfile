node {
       stage ('Clone') {
           step {
              sh 'Put clone command here'
           }
       }

       stage ('Build') {
           step {
              sh 'Put Build command here'
           }
       }
}
